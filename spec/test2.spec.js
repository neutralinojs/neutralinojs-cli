const assert = require('assert');

describe('Tests on Strings', function () {
  describe('string compare', function () {
    it('should return 1 if the given two strings are equal', function () {
      assert.equal("hmmm","hmmm");
    });
  });
});